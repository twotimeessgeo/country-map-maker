{
  "name": "geography-utility-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server 4173",
    "data:climate": "node scripts/sync-climate-data.mjs --write",
    "audit:climate:jma": "node scripts/audit-jma-stations.mjs",
    "check:climate": "node scripts/sync-climate-data.mjs --check",
    "check:js": "node --check app.js && node --check tools/climate/app.js && node --check tools/climate/korea-app.js && node --check tools/cut/cut.js",
    "check:site": "node scripts/check-static-site.mjs",
    "check": "npm run check:climate && npm run check:js && npm run check:site",
    "build": "npm run check && node scripts/build-static-site.mjs && node scripts/check-static-site.mjs dist"
  },
  "keywords": [
    "geography",
    "map",
    "climate",
    "exam"
  ],
  "license": "MIT",
  "description": "A static CSAT geography utility portal for maps, climate comparison, and cut prediction."
}
